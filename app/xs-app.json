{
  "welcomeFile": "orders/webapp/index.html",
  "authenticationMethod": "none",
  "sessionTimeout": 100,
  "pluginMetadataEndpoint": "/metadata",
  "routes": [
    {
      "source": "^/orders/webapp/(.*)",
      "target": "$1",
      "localDir": "orders/webapp/",
      "authenticationType": "none"
    },
    {
      "source": "^/(.*)$",
      "destination": "srv-api",
      "authenticationType": "none"
    }
  ]
}
